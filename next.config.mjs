/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['picsum.photos'],
    },
    compiler: {
        styledComponents: true,
      },
  
};

export default nextConfig;
